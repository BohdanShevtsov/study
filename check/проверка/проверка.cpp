
#include <iostream>

using namespace std;

int main(int argumentsCount,
    char* argumentValues[],
    char* environmentVariables[]
)
{
    std::cout << "Hello World!\n";
    int a = 10; 
    bool isOdd(int a);
    {
        return (a % 2 == 0) ? true : false;
    }
}

